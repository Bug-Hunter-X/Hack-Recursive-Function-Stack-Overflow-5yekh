function foo(x: int): int {
  var result: int = 1;
  for (var i = 1; i <= x; i++) {
    result *= i;
  }
  return result;
}

function main(): void {
  echo foo(5);
}
This iterative solution avoids the recursive calls, preventing stack overflow errors, even when calculating the factorial of large numbers.